# MySQL connection string
DATABASE_URL="mysql://user:password@localhost:3306/secrets_manager"

# 32-byte master key (64 hex chars)
MASTER_KEY="0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
MASTER_KEY_VERSION="v1"

# Web app origin for CORS and origin checks (set to your prod frontend URL in production)
APP_ORIGIN="http://localhost:5173"

# Session settings
SESSION_TTL_HOURS=168
COOKIE_SECURE=false
PORT=3001

# Logging
LOG_FORMAT=pretty
LOG_SINKS=console
LOG_REMOTE_ENABLED=false
# Reserved for future PostHog sink
# POSTHOG_HOST=https://us.i.posthog.com
# POSTHOG_API_KEY=phc_xxx

# Auth email delivery
AUTH_EMAIL_PROVIDER=noop
AUTH_EMAIL_FROM="auth@example.com"
AUTH_EMAIL_FROM_NAME="Secrets Auth"
# Required when AUTH_EMAIL_PROVIDER=resend
# RESEND_API_KEY="re_xxx"

# Runtime auth abuse protections
AUTH_LOGIN_MAX_ATTEMPTS=5
AUTH_LOGIN_LOCK_MS=300000

# OAuth runtime settings
AUTH_RUNTIME_BASE_URL="http://localhost:3001"
GOOGLE_OAUTH_SCOPES="openid,email,profile"
GITHUB_OAUTH_SCOPES="read:user,user:email"

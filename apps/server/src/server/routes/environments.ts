import type { FastifyInstance } from 'fastify';

export async function registerRoutes(_app: FastifyInstance): Promise<void> {
  // Route extraction target: implemented incrementally while preserving API behavior.
}
